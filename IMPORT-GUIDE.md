# راهنمای کامل ایمپورت محصولات زرگر

## مراحل نصب و راه‌اندازی

### مرحله 1: ساخت Attribute های Global

**مهم:** قبل از ایمپورت اولین محصول، باید این مرحله را انجام دهید.

```
http://localhost/wp/wp-content/plugins/zargar-accounting/setup-attributes.php
```

این فایل 9 attribute زیر را می‌سازد:

1. **وزن** (`pa_weight`) - نوع: text
2. **وزن پایه** (`pa_base_weight`) - نوع: text
3. **اجرت(درصد)** (`pa_wagepercent`) - نوع: text - محاسباتی
4. **اجرت** (`pa_wage`) - نوع: text
5. **سایز** (`pa_size`) - نوع: select
6. **مدل** (`pa_model`) - نوع: text
7. **رنگ** (`pa_color`) - نوع: select
8. **مجموعه** (`pa_collection`) - نوع: select
9. **نرخ وزن** (`pa__weight_symbol_rate`) - نوع: text

بعد از اجرا:
- به `WooCommerce > محصولات > ویژگی‌ها` بروید و آنها را مشاهده کنید
- به `تنظیمات > پیوندهای یکتا` بروید و دکمه "ذخیره تغییرات" را بزنید

---

## مرحله 2: تنظیمات پلاگین

در `پیشخوان > زرگر > تنظیمات`:

```
آدرس سرور: 37.235.18.235
پورت: 8090
نام کاربری: [username]
رمز عبور: [password]
```

**تست اتصال** را بزنید تا مطمئن شوید اتصال برقرار است.

---

## مرحله 3: ایمپورت محصولات

### روش 1: ایمپورت دسته‌ای
1. به `پیشخوان > زرگر > ایمپورت` بروید
2. فیلدهای مورد نظر را انتخاب کنید
3. دکمه "شروع ایمپورت" را بزنید

### روش 2: ایمپورت انتخابی
1. به `پیشخوان > زرگر > ایمپورت` بروید
2. به بخش "ایمپورت انتخابی" بروید
3. کدهای محصول را وارد کنید (هر خط یک کد)
4. دکمه "جستجو" را بزنید
5. فیلدهای مورد نظر را انتخاب کنید
6. محصولات را انتخاب کنید
7. دکمه "ایمپورت محصولات انتخاب شده" را بزنید

---

## ساختار فیلدها

### Meta Fields (14 فیلد)
این فیلدها در `post_meta` ذخیره می‌شوند:

| فیلد API | نام فارسی | Meta Key |
|---------|-----------|----------|
| LocationTitle | محل نگهداری | `_location` |
| ProductId | شناسه خارجی | `_external_id` |
| StonePrice | قیمت سنگ | `_stone_price` |
| WageOfPrice | قیمت اجرت | `wage_price` |
| IncomeTotal | مجموع درآمد | `_income_total` |
| TaxTotal | مجموع مالیات | `_tax_total` |
| SaleWageOfPercent | درصد اجرت فروش | `sale_wage_percent` |
| SaleWageOfPrice | مبلغ اجرت فروش | `sale_wage_price` |
| SaleWageOfPriceType | نوع اجرت فروش | `sale_wage_price_type` |
| SaleStonePrice | اجرت سنگ فروش | `sale_wage_stone` |
| OfficeCode | کد دفتر | `_office_code` |
| DesignerCode | کد طراح | `_designer_code` |
| other1 | فیلد اضافی 1 | `_extra_field_1` |
| other2 | فیلد اضافی 2 | `_extra_field_2` |

### Product Attributes (9 فیلد)
این فیلدها به عنوان WooCommerce Attributes ذخیره می‌شوند:

| فیلد API | نام فارسی | Attribute Slug | نوع |
|---------|-----------|----------------|-----|
| Weight | وزن | `pa_weight` | مستقیم از API |
| BaseWeight | وزن پایه | `pa_base_weight` | مستقیم از API |
| ModelTitle | مدل | `pa_model` | مستقیم از API |
| CollectionTitle | مجموعه | `pa_collection` | مستقیم از API |
| ColorTitle | رنگ | `pa_color` | مستقیم از API |
| SizeTitle | سایز | `pa_size` | مستقیم از API |
| WeightSymbolRate | نرخ وزن | `pa__weight_symbol_rate` | مستقیم از API |
| WageOfPrice | اجرت | `pa_wage` | مستقیم از API |
| calculated_wage_percent | اجرت(درصد) | `pa_wagepercent` | **محاسباتی** |

**فرمول محاسبه اجرت درصدی:**
```
اجرت درصدی = (اجرت فروش / قیمت کل) × 100
```

### فیلدهای استاندارد WooCommerce

| فیلد API | هدف WooCommerce |
|---------|-----------------|
| ProductCode | SKU |
| ProductTitle | عنوان محصول |
| GoldPrice | قیمت عادی |
| TotalPrice | قیمت فروش |
| IsExists | وضعیت موجودی |
| CategoryTitle | دسته‌بندی |
| DefaultImageURL | تصویر شاخص |
| ImageURL1-6 | گالری تصاویر |

---

## تصاویر

### نحوه کار
1. API تصویر را به صورت `/files/Img20251110-254.jpg` برمی‌گرداند
2. پلاگین URL کامل را می‌سازد: `http://37.235.18.235:8090/files/Img20251110-254.jpg`
3. تصویر دانلود و به WordPress Media Library اضافه می‌شود
4. برای جلوگیری از دانلود مجدد، نام فایل در دیتابیس جستجو می‌شود

### بررسی تصاویر
لاگ‌های تصویر در `error_log` WordPress ذخیره می‌شوند:

```
Zargar Image Success: Attached ID 123 | Product: 456 | URL: http://...
Zargar Image Download Error: ... | URL: http://...
Zargar Image: Using existing attachment ID 123 | URL: http://...
```

---

## تست

### تست ایمپورت یک محصول
```
http://localhost/wp/wp-content/plugins/zargar-accounting/test-import-single.php?code=GD01000312
```

این صفحه:
- محصول را از API جستجو می‌کند
- تمام فیلدها را ایمپورت می‌کند
- Meta fields و Attributes را جداگانه نمایش می‌دهد
- اطلاعات تصاویر را نشان می‌دهد

---

## مشکلات متداول

### 1. Attributes به عنوان Local ذخیره می‌شوند
**علت:** Attribute های Global ساخته نشده‌اند  
**راه‌حل:** فایل `setup-attributes.php` را اجرا کنید

### 2. تصاویر ایمپورت نمی‌شوند
**علت‌های ممکن:**
- آدرس IP یا پورت سرور اشتباه است
- فایروال دسترسی به سرور را مسدود کرده
- پوشه `wp-content/uploads` قابل نوشتن نیست

**بررسی:**
```bash
tail -f /path/to/wordpress/wp-content/debug.log | grep "Zargar Image"
```

### 3. محصول ایمپورت می‌شود اما فیلدها خالی است
**علت:** فیلدها در هنگام ایمپورت انتخاب نشده‌اند  
**راه‌حل:** در UI ایمپورت، حتماً فیلدها را انتخاب کنید

### 4. خطای "خطا در ارتباط با سرور"
**علت:** اطلاعات سرور اشتباه است یا سرور در دسترس نیست  
**راه‌حل:** تست اتصال را در صفحه تنظیمات انجام دهید

---

## کد نمونه

### ایمپورت برنامه‌نویسی
```php
use ZargarAccounting\Admin\ProductImportManager;
use ZargarAccounting\Helpers\FieldMapper;

$manager = ProductImportManager::getInstance();

// جستجوی محصولات
$products = $manager->searchProductsByCodes(['GD01000312', 'GD01000313']);

// دریافت تمام فیلدها
$allFields = FieldMapper::getAvailableFields();
$selectedFields = [];
foreach ($allFields as $category) {
    foreach ($category['fields'] as $fieldName => $info) {
        $selectedFields[] = $fieldName;
    }
}

// ایمپورت هر محصول
foreach ($products as $productData) {
    $manager->importSingleProduct($productData, $selectedFields);
    echo "محصول {$productData['ProductCode']} ایمپورت شد\n";
}
```

### افزودن Meta Field سفارشی
در `FieldMapper.php`:

```php
'custom_category' => [
    'title' => 'فیلدهای سفارشی',
    'fields' => [
        'CustomField' => ['label' => 'فیلد من', 'target' => 'meta:_my_custom_field'],
    ]
],
```

### افزودن Attribute جدید
1. در `setup-attributes.php` اضافه کنید:
```php
'my_attr' => [
    'name' => 'ویژگی من',
    'slug' => 'my_attr',
    'type' => 'text',
    'order_by' => 'menu_order'
]
```

2. در `FieldMapper.php` اضافه کنید:
```php
'MyApiField' => ['label' => 'فیلد من', 'target' => 'attribute:my_attr'],
```

3. فایل `setup-attributes.php` را اجرا کنید

---

## فایل‌های مهم

```
zargar-accounting/
├── zargar-accounting.php              # فایل اصلی پلاگین
├── setup-attributes.php               # ساخت attribute های global (یکبار اجرا)
├── test-import-single.php             # تست ایمپورت یک محصول
├── includes/
│   ├── Admin/
│   │   ├── ProductImportManager.php   # مدیریت ایمپورت
│   │   ├── ProductMetaBox.php         # نمایش meta fields در صفحه محصول
│   │   └── ImportHandler.php          # هندلر ذخیره meta/attribute
│   └── Helpers/
│       └── FieldMapper.php            # نقشه‌برداری فیلدها
└── assets/
    └── js/
        └── import.js                  # UI ایمپورت
```

---

## پشتیبانی

- 📧 ایمیل: support@example.com
- 📚 مستندات: [لینک]
- 🐛 گزارش باگ: [لینک GitHub]

---

تاریخ به‌روزرسانی: 2025-12-17
نسخه: 2.0.0
