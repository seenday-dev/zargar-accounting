# 🚀 راهنمای سریع نصب - سیستم لاگ جدید

## برای تست فوری در WordPress:

### 1. آپلود فایل‌ها (4 فایل)

```bash
# فایل‌های جدید:
✅ includes/Admin/LogsViewer.php
✅ templates/admin/simple-logs.php

# فایل‌های ویرایش شده:
✅ includes/Admin/MenuManager.php
✅ includes/Admin/AssetsManager.php
```

### 2. پاک کردن کش

اگر از plugin کش استفاده می‌کنید:
- WP Super Cache → Delete Cache
- W3 Total Cache → Performance → Empty All Caches
- یا هر کش دیگر

### 3. تست

بازدید از:
```
wp-admin/admin.php?page=zargar-accounting-logs
```

**نتیجه انتظاری**:
- ✅ صفحه فوری بارگذاری می‌شود (نه "در حال بارگذاری...")
- ✅ فونت ایران‌یکان
- ✅ آمارها عدد دارند
- ✅ تب‌ها کلیک‌پذیر هستند
- ✅ آیکون‌های داشبورد کار می‌کنند

---

## اگر مشکلی بود:

### خطای PHP:
```php
// در wp-config.php اضافه کنید:
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);

// سپس بررسی کنید:
// wp-content/debug.log
```

### لاگ‌ها خالی است:
```bash
# در ترمینال سرور:
cd /path/to/plugin
php generate-test-logs.php
```

### فونت اعمال نمی‌شود:
- بررسی کنید CDN jsdelivr در ایران فیلتر نباشد
- اگر فیلتر است، از VPN استفاده کنید

---

## تماس برای پشتیبانی:

اگر مشکلی پیش آمد، فایل‌های زیر را چک کنید:
- `SIMPLE-REWRITE.md` - مستندات کامل
- `test-simple-viewer.php` - تست خودکار
- `TROUBLESHOOTING.md` - راهنمای رفع مشکل

---

**موفق باشید! 🎉**
