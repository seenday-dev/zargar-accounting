# ✅ چک‌لیست نصب و تست - سیستم لاگ جدید

## 📦 مرحله 1: بررسی فایل‌ها

### فایل‌های جدید (باید آپلود شوند):
- [ ] `includes/Admin/LogsViewer.php` (3.1 KB)
- [ ] `templates/admin/simple-logs.php` (8.6 KB)
- [ ] `test-simple-viewer.php` (3.8 KB) - اختیاری، فقط برای تست
- [ ] `SIMPLE-REWRITE.md` (13 KB) - مستندات
- [ ] `QUICK-INSTALL.md` (1.8 KB) - راهنما
- [ ] `CHANGES-SUMMARY.md` (4.5 KB) - خلاصه
- [ ] `ARCHITECTURE.md` (9.7 KB) - معماری

### فایل‌های ویرایش شده (باید جایگزین شوند):
- [ ] `includes/Admin/MenuManager.php` - متد `renderLogs()` تغییر کرد
- [ ] `includes/Admin/AssetsManager.php` - اضافه شدن `dashicons`

---

## 🧪 مرحله 2: تست محلی (قبل از آپلود)

```bash
cd /path/to/plugin
php test-simple-viewer.php
```

**خروجی انتظاری**:
```
✅ Instance created
✅ All format methods working
✅ Logs fetched successfully
✅ Template file exists
```

اگر هر یک از تست‌ها ❌ بود، **آپلود نکنید!**

---

## 📤 مرحله 3: آپلود به سرور

### روش 1: FTP/SFTP
```
1. اتصال به سرور
2. رفتن به: /wp-content/plugins/zargar-accounting/
3. آپلود فایل‌های جدید
4. جایگزینی فایل‌های ویرایش شده
```

### روش 2: SSH
```bash
# کپی فایل‌ها به سرور
scp -r includes/Admin/LogsViewer.php user@server:/path/to/plugin/includes/Admin/
scp -r templates/admin/simple-logs.php user@server:/path/to/plugin/templates/admin/
scp -r includes/Admin/MenuManager.php user@server:/path/to/plugin/includes/Admin/
scp -r includes/Admin/AssetsManager.php user@server:/path/to/plugin/includes/Admin/
```

### روش 3: Git (اگر از version control استفاده می‌کنید)
```bash
git add .
git commit -m "feat: Complete rewrite of logs system - Remove AJAX, use simple PHP"
git push origin main

# سپس در سرور:
cd /path/to/plugin
git pull origin main
```

---

## 🔧 مرحله 4: پیکربندی سرور

### 4.1. بررسی مجوزها (Permissions)
```bash
# در سرور:
chmod 755 includes/Admin/LogsViewer.php
chmod 755 templates/admin/simple-logs.php
chmod 755 includes/Admin/MenuManager.php
chmod 755 includes/Admin/AssetsManager.php

# مجوزهای storage/logs
chmod -R 777 storage/logs/
```

### 4.2. پاک کردن کش PHP (OPcache)
```bash
# روش 1: Restart PHP-FPM
sudo systemctl restart php8.1-fpm

# روش 2: از طریق PHP
php -r "opcache_reset();"

# روش 3: از طریق WordPress
# wp-admin → Tools → Clear Cache
```

### 4.3. پاک کردن کش WordPress
```
- WP Super Cache: Performance → Delete Cache
- W3 Total Cache: Performance → Empty All Caches
- Cloudflare: Purge Everything
- یا هر plugin کش دیگری
```

---

## 🌐 مرحله 5: تست در WordPress

### 5.1. بررسی خطاهای PHP

**فعال کردن Debug Mode**:
```php
// در wp-config.php
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
define('WP_DEBUG_DISPLAY', false);
```

**بررسی Log**:
```bash
tail -f wp-content/debug.log
```

**اگر خطایی دیدید**, به بخش "رفع مشکلات" بروید.

---

### 5.2. بازدید از صفحه لاگ‌ها

**URL**:
```
https://your-site.com/wp-admin/admin.php?page=zargar-accounting-logs
```

**چک‌لیست بصری**:
- [ ] صفحه فوری بارگذاری شد (< 2 ثانیه)
- [ ] نوشته "در حال بارگذاری..." نمایش داده **نمی‌شود**
- [ ] فونت ایران‌یکان در **همه جا** اعمال شده
- [ ] 4 تب نمایش داده می‌شود: محصولات، فروش، قیمت، خطاها
- [ ] هر تب badge تعداد دارد (نه صفر)
- [ ] جدول لاگ‌ها نمایش داده می‌شود
- [ ] رنگ‌های سطوح (INFO/ERROR/...) صحیح است

---

### 5.3. تست تعامل کاربر

**کلیک روی تب "فروش"**:
- [ ] صفحه بارگذاری مجدد شد
- [ ] URL تغییر کرد به: `...&channel=sales`
- [ ] لاگ‌های فروش نمایش داده شد
- [ ] badge فعال شد روی تب "فروش"

**کلیک روی تب "قیمت"**:
- [ ] صفحه بارگذاری مجدد شد
- [ ] URL تغییر کرد به: `...&channel=price`
- [ ] لاگ‌های قیمت نمایش داده شد

**کلیک روی تب "خطاها"**:
- [ ] صفحه بارگذاری مجدد شد
- [ ] لاگ‌های خطا نمایش داده شد

---

### 5.4. تست آیکون‌های داشبورد

**بازدید از داشبورد**:
```
https://your-site.com/wp-admin/admin.php?page=zargar-accounting
```

**چک‌لیست**:
- [ ] آیکون "تراکنش امروز" نمایش داده می‌شود
- [ ] آیکون "نرخ موفقیت" نمایش داده می‌شود
- [ ] آیکون "آخرین همگام‌سازی" نمایش داده می‌شود

**اگر آیکون‌ها نمایش داده نشدند**:
1. `F12` → Network → Filter: CSS
2. جستجو: `dashicons`
3. اگر 404 بود → مشکل از WordPress
4. راه‌حل موقت در `SIMPLE-REWRITE.md` > "رفع مشکلات"

---

## 🔍 مرحله 6: بررسی Browser Console

**باز کردن Console**:
```
F12 → Console
```

**چک‌لیست**:
- [ ] **هیچ خطای قرمز** نمایش داده نمی‌شود
- [ ] پیام "logs.js loaded" نمایش داده **نمی‌شود** (چون حذف شده)
- [ ] درخواست AJAX به `admin-ajax.php` ارسال **نمی‌شود**

**اگر خطایی دیدید**: به بخش "رفع مشکلات" مراجعه کنید.

---

## 🔍 مرحله 7: بررسی Network

**باز کردن Network Tab**:
```
F12 → Network → Reload صفحه
```

**چک‌لیست**:
- [ ] فقط **1 درخواست** به سرور ارسال شد (نه 2 یا بیشتر)
- [ ] هیچ درخواستی به `admin-ajax.php` ارسال **نشد**
- [ ] `yekan-font.min.css` بارگذاری شد (200 OK)
- [ ] `dashicons.min.css` بارگذاری شد (200 OK)
- [ ] `simple-logs.php` render شد (نه `logs.blade.php`)

---

## 📊 مرحله 8: تست عملکرد

### 8.1. تست با لاگ‌های خالی

```bash
# در سرور:
rm -f storage/logs/*/*.log
```

**سپس بازدید از صفحه لاگ‌ها**:
- [ ] پیام "لاگی موجود نیست" نمایش داده می‌شود
- [ ] badge‌ها همه صفر هستند
- [ ] هیچ خطایی نمایش داده نمی‌شود

---

### 8.2. تولید لاگ‌های تستی

```bash
# در سرور:
php generate-test-logs.php
```

**سپس بازدید از صفحه لاگ‌ها**:
- [ ] badge‌ها عدد دارند
- [ ] لاگ‌ها در جدول نمایش داده می‌شوند
- [ ] رنگ‌ها و فرمت‌ها صحیح هستند

---

### 8.3. تست با حجم زیاد

```bash
# تولید 1000 لاگ
for i in {1..1000}; do
    php -r "ZargarAccounting\Logger\MonologManager::getInstance()->product('Test log #$i');"
done
```

**بررسی عملکرد**:
- [ ] صفحه در کمتر از 2 ثانیه بارگذاری شد
- [ ] فقط 100 لاگ اخیر نمایش داده شد (default limit)
- [ ] اسکرول کار می‌کند
- [ ] هیچ خطا یا warning نیست

---

## 🐛 مرحله 9: رفع مشکلات

### مشکل 1: صفحه سفید (500 Error)

**علت**: خطای PHP

**راه‌حل**:
```bash
# بررسی error log
tail -f /var/log/php8.1-fpm.log
tail -f wp-content/debug.log

# اگر خطای syntax دیدید:
php -l includes/Admin/LogsViewer.php
php -l templates/admin/simple-logs.php
```

---

### مشکل 2: "Class 'LogsViewer' not found"

**علت**: autoload درست کار نمی‌کند

**راه‌حل**:
```bash
# در دایرکتوری plugin:
composer dump-autoload -o
```

---

### مشکل 3: لاگ‌ها نمایش داده نمی‌شوند

**علت**: فایل‌های لاگ خالی هستند

**راه‌حل**:
```bash
php generate-test-logs.php
php check-structure.php
```

---

### مشکل 4: فونت اعمال نمی‌شود

**علت**: CDN jsdelivr فیلتر است

**راه‌حل**:
1. **موقت**: از VPN استفاده کنید
2. **دائم**: فایل فونت را local قرار دهید

```php
// در simple-logs.php
<style>
@font-face {
    font-family: 'yekan';
    src: url('/path/to/yekan.woff2') format('woff2');
}
</style>
```

---

### مشکل 5: آیکون‌های داشبورد 404

**علت**: dashicons موجود نیست

**راه‌حل**:
```bash
# بررسی فایل
ls wp-includes/css/dashicons.min.css

# اگر نبود:
wp core download --force --skip-content
```

---

## ✅ مرحله 10: تایید نهایی

### چک‌لیست کامل:

#### عملکرد:
- [ ] صفحه لاگ‌ها فوری بارگذاری می‌شود
- [ ] تمام تب‌ها کلیک‌پذیر هستند
- [ ] لاگ‌ها با فرمت صحیح نمایش داده می‌شوند
- [ ] آمارها عدد واقعی دارند
- [ ] آیکون‌های داشبورد کار می‌کنند

#### ظاهر:
- [ ] فونت ایران‌یکان در همه جا اعمال شده
- [ ] رنگ‌بندی صحیح است
- [ ] طراحی مدرن و تمیز است
- [ ] Responsive است (روی موبایل هم کار می‌کند)

#### فنی:
- [ ] هیچ خطای PHP در log نیست
- [ ] هیچ خطای JavaScript در console نیست
- [ ] هیچ درخواست AJAX ارسال نمی‌شود
- [ ] فقط 1 درخواست HTTP برای هر صفحه
- [ ] کش مشکلی ایجاد نمی‌کند

---

## 🎉 اگر همه چک‌لیست‌ها OK بود:

**تبریک! سیستم با موفقیت نصب شد! 🚀**

---

## 📞 در صورت مشکل:

1. بررسی `SIMPLE-REWRITE.md` → بخش "رفع مشکلات"
2. بررسی `TROUBLESHOOTING.md`
3. بررسی `wp-content/debug.log`
4. بررسی PHP error log
5. تست مجدد با `test-simple-viewer.php`

---

**تاریخ**: ۱۴۰۳/۰۹/۲۵
**نسخه**: 2.1.0
**وضعیت**: ✅ آماده Production
