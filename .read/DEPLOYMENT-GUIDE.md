# 🚀 راهنمای استقرار نهایی - افزونه زرگر حسابداری

## ✅ چک‌لیست قبل از استقرار

### 1️⃣ فایل‌های کلیدی موجود باشند:

```bash
# در پوشه افزونه بررسی کنید:
ls -lh assets/icons/
```

باید این فایل‌ها را ببینید:
- ✅ `lineicons-fixed.css` (2-3 KB)
- ✅ `LineIcons.woff2` (71 KB)
- ✅ `LineIcons.woff` (87 KB)

### 2️⃣ فایل‌های تغییر یافته:

این فایل‌ها باید به‌روز شده باشند:
- ✅ `includes/Admin/AssetsManager.php` - اضافه شدن lineicons-fixed.css
- ✅ `templates/components/header.blade.php` - استفاده از lineicons-fixed.css
- ✅ `templates/admin/simple-logs.php` - استفاده از lineicons-fixed.css

---

## 📋 مراحل استقرار

### مرحله 1: آپلود فایل‌ها

1. **از طریق FTP/SFTP:**
   ```
   /wp-content/plugins/zargar-accounting/
   ├── assets/icons/
   │   ├── lineicons-fixed.css  ← جدید
   │   ├── LineIcons.woff2      ← جدید
   │   └── LineIcons.woff       ← جدید
   ├── includes/Admin/
   │   ├── AssetsManager.php    ← به‌روز شده
   │   └── LogsViewer.php       ← جدید
   └── templates/admin/
       └── simple-logs.php      ← جدید
   ```

2. **یا از طریق SSH:**
   ```bash
   cd /path/to/wordpress/wp-content/plugins/
   # حذف نسخه قدیمی
   rm -rf zargar-accounting
   # آپلود نسخه جدید
   # سپس تنظیم مجوزها:
   chmod -R 755 zargar-accounting
   chown -R www-data:www-data zargar-accounting
   ```

### مرحله 2: تست در محیط Development

قبل از استقرار در Production، حتماً تست کنید:

1. **تست فایل‌ها:**
   - فایل `test-wp-assets.php` را در روت وردپرس قرار دهید
   - به آدرس `http://your-site.com/test-wp-assets.php` بروید
   - همه چیز باید ✅ سبز باشه

2. **تست در مرورگر:**
   - وارد پنل ادمین شوید
   - به صفحه افزونه بروید
   - Console مرورگر را باز کنید (F12)
   - نباید خطای 404 برای فایل‌های CSS/Font ببینید

### مرحله 3: پاک کردن Cache

```bash
# 1. Cache وردپرس
wp cache flush

# 2. Cache مرورگر
# در Chrome: Ctrl+Shift+R
# در Firefox: Ctrl+F5

# 3. اگر از CDN استفاده می‌کنید (مثل Cloudflare)
# حتماً Cache را Purge کنید
```

### مرحله 4: فعال‌سازی افزونه

1. به بخش افزونه‌ها بروید
2. افزونه را غیرفعال کنید
3. دوباره فعال کنید (برای اطمینان از بارگذاری فایل‌های جدید)

---

## 🔍 عیب‌یابی

### مشکل 1: آیکون‌ها هنوز نمایش داده نمی‌شوند

**بررسی 1 - دسترسی به فایل‌ها:**
```bash
# URL های زیر را در مرورگر باز کنید:
http://your-site.com/wp-content/plugins/zargar-accounting/assets/icons/lineicons-fixed.css
http://your-site.com/wp-content/plugins/zargar-accounting/assets/icons/LineIcons.woff2
```

اگر 404 گرفتید:
- ✅ مجوزهای فایل را بررسی کنید (`chmod 644`)
- ✅ مالکیت فایل را بررسی کنید (`chown www-data:www-data`)
- ✅ مسیر فایل‌ها را دوباره چک کنید

**بررسی 2 - Console مرورگر:**
```
F12 → Console Tab
```

دنبال این خطاها بگردید:
- ❌ `Failed to load resource: 404` → مسیر فایل اشتباه است
- ❌ `net::ERR_BLOCKED_BY_CLIENT` → AdBlock یا فیلترشکن مشکل دارد
- ❌ `CORS error` → مشکل Cross-Origin

**بررسی 3 - Network Tab:**
```
F12 → Network Tab → فیلتر CSS/Font
```

باید این فایل‌ها را با Status 200 ببینید:
- ✅ `lineicons-fixed.css` - Status: 200
- ✅ `LineIcons.woff2` - Status: 200

**بررسی 4 - Computed Styles:**
```
F12 → Elements → انتخاب یک <i class="lni lni-home">
→ Computed Tab → جستجوی 'font-family'
```

باید ببینید:
```
font-family: 'LineIcons' !important
```

### مشکل 2: فونت یکان اعمال نمی‌شود

**راه‌حل:**
در فایل `simple-logs.php` در تگ `<style>` این کد را اضافه کنید:
```css
* {
    font-family: 'yekan', 'Tahoma', 'Arial', sans-serif !important;
}
```

### مشکل 3: صفحه لاگ‌ها بی‌نهایت لود می‌شود

این مشکل در نسخه جدید حل شده. اما اگر هنوز دارید:

1. کش مرورگر را پاک کنید
2. مطمئن شوید `LogsViewer.php` جدید آپلود شده
3. در `MenuManager.php` باید از `LogsViewer` استفاده شود نه `LoggerAjax`

---

## ✅ تست نهایی

بعد از استقرار، این موارد را چک کنید:

### 1. صفحه Dashboard
```
✅ آیکون‌ها نمایش داده می‌شوند
✅ فونت یکان اعمال شده
✅ استایل‌ها درست لود شده‌اند
```

### 2. صفحه Logs
```
✅ سریع لود می‌شود (< 2 ثانیه)
✅ لاگ‌ها نمایش داده می‌شوند
✅ فیلترها کار می‌کنند
✅ Pagination کار می‌کند
```

### 3. Console مرورگر
```
✅ هیچ خطای 404 نیست
✅ هیچ خطای JavaScript نیست
✅ پیغام "Gold Theme Initialized" نمایش داده می‌شود
```

---

## 📊 مقایسه قبل/بعد

| مورد | قبل | بعد |
|------|-----|-----|
| آیکون‌ها | ❌ نمایش داده نمی‌شدند | ✅ کامل کار می‌کنند |
| فونت | ❌ یکان اعمال نمی‌شد | ✅ در همه‌جا یکان است |
| صفحه لاگ | ❌ بی‌نهایت لود | ✅ سریع (< 2s) |
| CDN | ❌ فیلتر شده | ✅ محلی و مستقل |
| حجم | - | ✅ فقط 161 KB اضافه |

---

## 🔒 نکات امنیتی

1. **مجوزها:**
   ```bash
   chmod 644 assets/icons/*.css
   chmod 644 assets/icons/*.woff*
   chmod 755 assets/icons/
   ```

2. **htaccess:**
   اگر نیاز است، این را در `.htaccess` اضافه کنید:
   ```apache
   <FilesMatch "\.(woff|woff2|ttf|eot)$">
     Header set Access-Control-Allow-Origin "*"
   </FilesMatch>
   ```

3. **Cache Headers:**
   برای بهینه‌سازی، Cache Headers را تنظیم کنید:
   ```apache
   <FilesMatch "\.(css|woff|woff2)$">
     Header set Cache-Control "max-age=31536000, public"
   </FilesMatch>
   ```

---

## 📞 پشتیبانی

اگر مشکلی بود:

1. **لاگ‌های PHP را بررسی کنید:**
   ```bash
   tail -f /var/log/apache2/error.log
   # یا
   tail -f /var/log/nginx/error.log
   ```

2. **Debug Mode را فعال کنید:**
   در `wp-config.php`:
   ```php
   define('WP_DEBUG', true);
   define('WP_DEBUG_LOG', true);
   define('WP_DEBUG_DISPLAY', false);
   ```

3. **Console مرورگر را چک کنید:**
   - همیشه F12 را باز نگه دارید
   - تب Network را مانیتور کنید
   - تب Console را برای خطاها بررسی کنید

---

## 🎉 استقرار موفقیت‌آمیز!

اگر همه چک‌لیست‌ها ✅ هستند، تبریک می‌گویم! 🎊

افزونه شما آماده استفاده در Production است.

**نکته:** فایل `test-wp-assets.php` را بعد از تست حذف کنید (برای امنیت).

---

*آخرین به‌روزرسانی: ۲۶ آذر ۱۴۰۴*
